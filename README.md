# World Industry Atlas

Static prototype for visualizing global industries and major public companies.

- `index.html`: industry overview map
- `company-heatmap.html`: weekly global company heatmap

The heatmap uses local weekly snapshot data for the prototype. Production data updates should be handled by a server-side scheduled job that writes static JSON for the page to consume.
